# discoball-automation
# DiscoBall – Android UI Automation Tool  DiscoBall is a lightweight Android automation tool that performs repetitive UI interactions using image detection instead of fixed screen coordinates.  The app analyzes the visible screen state and decides which action to perform based on detected UI elements.
